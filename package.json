{
  "name": "bagina-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Bagino Bagina — cute multiplayer board game (workspace root)",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "gen": "pnpm --filter @bagina/schema run gen && pnpm --filter @bagina/theme run gen",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "build": "pnpm -r run build",
    "playtest": "pnpm --filter server run playtest"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
